apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: {{ $.Values.applicationName }}
  name: {{ .Release.Name }}-collections
spec:
  storageClassName: {{ .Release.Name }}-sc
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ $.Values.gdsc.storage.collections.persistence.size }}
