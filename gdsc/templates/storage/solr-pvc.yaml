apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: {{ $.Values.applicationName }}
  name: {{ .Release.Name }}-solr
spec:
  storageClassName: {{ .Release.Name }}-sc
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ $.Values.gdsc.storage.solr.persistence.size }}
