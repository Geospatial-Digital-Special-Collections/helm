apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgis-vocabulary-ceph
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests: 
      storage: 100Mi
  volumeName: local-postgis-vocabulary