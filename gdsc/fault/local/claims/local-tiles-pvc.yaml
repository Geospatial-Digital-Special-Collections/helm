apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tiles-ceph
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests: 
      storage: 20Gi
  volumeName: local-tiles