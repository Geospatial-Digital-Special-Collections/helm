apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: {{ $.Values.applicationName }}
  name: {{ .Release.Name }}-data-store
spec:
  storageClassName: {{ $.Values.dataStore.persistence.storageClassName }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ $.Values.dataStore.persistence.size }}
